/** @fileoverview The application code, which mentions none of the ports below. */

/** Says hello. The point of this file is that no port in the stack appears in it. */
export function hello(): string {
  return 'hello';
}
