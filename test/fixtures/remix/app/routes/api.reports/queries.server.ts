/**
 * A file the route above keeps beside itself. Inside a route folder only `route.tsx`
 * answers a URL — which is the entire point of the folder.
 */
export async function listReports(): Promise<string[]> {
  return [];
}
