"""The old overview, kept for reference and run by nobody.

A door somebody cannot use is not a way in. powerfab-dashboard reported 111 of them,
among which every retired script in `scripts/categories/_archive/`.
"""


def overview(rows):
    return len(rows)


if __name__ == "__main__":
    print(overview([]))
