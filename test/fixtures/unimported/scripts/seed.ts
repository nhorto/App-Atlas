/**
 * @fileoverview Fills the database for a fresh checkout. Nothing imports it; the
 * `seed` script in package.json runs it, which is a declaration in writing.
 */
export function seed(): number {
  return 42;
}
