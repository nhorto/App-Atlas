/**
 * @fileoverview Nothing but the test suite imports this. That is a different fact from
 * "nothing imports it", and this fixture exists so the two never get merged.
 */
export function helper(n: number): number {
  return n * 2;
}
