/** @fileoverview A second package, so this reads as a workspace worth a switcher. */

/** Trims a string. Here only so the workspace has more than one package in it. */
export function tidy(value: string): string {
  return value.trim();
}
