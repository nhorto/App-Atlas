import { Text, View } from 'react-native';

// Under the `(tabs)` group, which shapes navigation but not the URL: this is `/home`.
export default function HomeTab() {
  return (
    <View>
      <Text>Home</Text>
    </View>
  );
}
