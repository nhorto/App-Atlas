/**
 * @fileoverview The only file here, and it is documented.
 *
 * The fixture exists to make coverage exactly 100%, which is the case that used to be
 * told to go and learn how to write docstrings (#124).
 */

/** Says hello to somebody. */
export function greet(name: string): string {
  return `hello ${name}`;
}
