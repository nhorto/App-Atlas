// Rocket.Chat's, verbatim. The reason every address the suite writes is a fragment.
export const BASE_URL = 'http://localhost:3000';
export const API_PREFIX = '/api/v1';
export const BASE_API_URL = BASE_URL + API_PREFIX;
