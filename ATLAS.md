# app-atlas — map

> Generated by App Atlas v0.5.0 on 2026-07-25. Facts are derived by each language's own parser and type checker; sentences marked `(ai)` were generated and may be wrong. Re-run `app-atlas export` after the code changes.

## By the numbers

- 76 files · 18,914 lines · 15 folders · 528 functions · 222 types
- 3 ways in · 0 outside services · 1 data store · 7 env vars
- Built with React, Vite
- 99% of files carry a docstring App Atlas reads verbatim

## Also runs on its own

- **cli** Command line — `src/cli.ts`
- **file-read** Files on disk — `src/analyze/cache.ts`

## Where data goes

- **Files on disk** (Node fs) — 0 reads, 40 writes

## The parts

- `scripts` (1 file, logic)
- `src` (53 files, logic)
- `test` (7 files, test)
- `web/src` (14 files, ui)

## Key types

- **AtlasNode** (interface, `src/model/types.ts`) — used in 128 places — id, kind, name, label, parentId, language, path, startLine, endLine, zone, summary, summarySource
- **DetectorContext** (interface, `src/analyze/boundaries/types.ts`) — used in 59 places — ref, sf, fileId, project, signals, imports, packages, locals
- **AtlasEdge** (interface, `src/model/types.ts`) — used in 48 places — id, kind, fromId, toId, weight, confidence, provenance, meta
- **Zone** (type-alias, `src/model/types.ts`) — used in 41 places — 'ui' | 'api' | 'logic' | 'data' | 'config' | 'test' | 'unknown'
- **AtlasGraph** (class, `src/model/graph.ts`) — used in 36 places — meta, rootId, nodes, children, relations, incoming, outgoing, pathCache
- **Atlas** (interface, `src/model/types.ts`) — used in 34 places — meta, nodes, edges
- **AtlasNode** (interface, `web/src/types.ts`) — used in 33 places — id, kind, name, label, parentId, language, path, startLine, endLine, zone, summary, summarySource
- **CodeSite** (interface, `src/model/types.ts`) — used in 32 places — path, line, nodeId, snippet?
- **SourceFileRef** (interface, `src/analyze/project.ts`) — used in 31 places — absPath, relPath, zone
- **BoundaryFinding** (type-alias, `src/analyze/boundaries/types.ts`) — used in 31 places — | EndpointFinding | ServiceFinding | StoreFinding | EnvFinding | GuardFinding | WebhookFinding
- **EndpointMeta** (interface, `src/model/types.ts`) — used in 25 places — endpointKind, method, route, framework, guards, writes, sites, schedule?, vars?, envExample?
- **EnrichBackend** (interface, `src/enrich/types.ts`) — used in 21 places — id, label, billing, pricing?, model?, concurrency
- **GuardInfo** (interface, `src/model/types.ts`) — used in 21 places — name, how, provider, path, line, confidence
- **BoundaryDetector** (interface, `src/analyze/boundaries/types.ts`) — used in 17 places — id
- **LevelNode** (interface, `web/src/types.ts`) — used in 14 places — childCount, drillable, outsideIn, outsideOut, preview
- **EnrichRequest** (interface, `src/enrich/types.ts`) — used in 13 places — system, user, maxOutputTokens
- **FieldInfo** (interface, `src/model/types.ts`) — used in 13 places — name, type, optional, isId?, isUnique?
- **FileSlice** (interface, `src/analyze/plugin.ts`) — used in 13 places — relPath, hash, nodes, edges, boundaries, positions, imports
- **LabelItem** (interface, `src/enrich/prompts.ts`) — used in 12 places — key, path, zone, contains, responsibilities
- **StoreMeta** (interface, `src/model/types.ts`) — used in 12 places — storeKind, client, tables, reads, writes, sites
- **Tour** (interface, `web/src/types.ts`) — used in 12 places — id, title, subtitle, kind, steps
- **EnrichReport** (interface, `src/enrich/index.ts`) — used in 11 places — backend, backendLabel, pendingItems, labelled, described, reusedFromCache, requests, failedRequests, filesSkipped, declined, usage, additions
- **ProjectInfo** (interface, `src/analyze/project.ts`) — used in 11 places — root, name, tsConfigPath, packageJson, files, frameworks, signals, workspaces, ignored, warnings
- **ServiceMeta** (interface, `src/model/types.ts`) — used in 11 places — category, packages, hosts, sites, external
- …and 198 more types.

## Environment variables

`ANTHROPIC_API_KEY`*, `ANTHROPIC_MODEL`*, `APP_ATLAS_DEBUG`*, `APP_ATLAS_PYTHON`*, `OPENAI_API_KEY`*, `OPENAI_BASE_URL`*, `OPENAI_MODEL`*

\* not in .env.example — 7 of 7 are read by the code but written down nowhere.

## Where to look first

The most connected files, which is usually where the app actually lives.

- `src/model/types.ts` (650 connections) — The Atlas data model.
- `web/src/types.ts` (346 connections) — Wire types.
- `src/analyze/ts/index.ts` (251 connections) — The TypeScript/JavaScript analyzer — the facts layer.
- `src/analyze/boundaries/build.ts` (209 connections) — Findings in, atlas nodes and edges out.
- `src/model/graph.ts` (207 connections) — In-memory query layer over an atlas.
- `src/analyze/boundaries/types.ts` (202 connections) — What a boundary detector produces, and what it is given to work with.
- `src/analyze/py/index.ts` (198 connections) — The Python analyzer — the second language plugin.
- `src/model/store.ts` (195 connections) — The atlas store.

---

This file is generated. The full atlas — every file, function, type and edge — is in `.app-atlas/atlas.json`, and `app-atlas serve` opens the same information as a map you can click through.
