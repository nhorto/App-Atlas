# app-atlas — map

> Generated by App Atlas v0.4.0 on 2026-07-25. Facts are derived from the TypeScript compiler; sentences marked `(ai)` were generated and may be wrong. Re-run `app-atlas export` after the code changes.

## By the numbers

- 76 files · 15,402 lines · 28 folders · 438 functions · 195 types
- 3 ways in · 4 outside services · 1 data store · 9 env vars
- Built with React, Vite
- 86% of files carry a docstring App Atlas reads verbatim · 34 describe code that has since changed

## Also runs on its own

- **cli** Command line — `src/cli.ts`
- **file-read** Files on disk — `src/analyze/project.ts`

## Where data goes

- **Files on disk** (Node fs) — 0 reads, 13 writes
- **Clerk** (auth) — 3 call sites via @clerk/nextjs
- **PostHog** (analytics) — 1 call site via api.us.posthog.com
- **Resend** (email) — 1 call site via resend
- **Stripe** (payments) — 1 call site via stripe

## The parts

- `src` (44 files, logic)
- `test` (17 files, test)
- `web/src` (14 files, ui)

## Key types

- **AtlasNode** (interface, `src/model/types.ts`) — used in 113 places — id, kind, name, label, parentId, language, path, startLine, endLine, zone, summary, summarySource
- **DetectorContext** (interface, `src/analyze/boundaries/types.ts`) — used in 59 places — ref, sf, fileId, project, signals, imports, packages, locals
- **AtlasEdge** (interface, `src/model/types.ts`) — used in 36 places — id, kind, fromId, toId, weight, confidence, provenance, meta
- **Zone** (type-alias, `src/model/types.ts`) — used in 40 places — 'ui' | 'api' | 'logic' | 'data' | 'config' | 'test' | 'unknown'
- **AtlasGraph** (class, `src/model/graph.ts`) — used in 34 places — meta, rootId, nodes, children, relations, incoming, outgoing, pathCache
- **AtlasNode** (interface, `web/src/types.ts`) — used in 33 places — id, kind, name, label, parentId, language, path, startLine, endLine, zone, summary, summarySource
- **Atlas** (interface, `src/model/types.ts`) — used in 30 places — meta, nodes, edges
- **EndpointMeta** (interface, `src/model/types.ts`) — used in 25 places — endpointKind, method, route, framework, guards, writes, sites, schedule?, vars?, envExample?
- **CodeSite** (interface, `src/model/types.ts`) — used in 22 places — path, line, nodeId, snippet?
- **EnrichBackend** (interface, `src/enrich/types.ts`) — used in 21 places — id, label, billing, pricing?, model?, concurrency
- **SourceFileRef** (interface, `src/analyze/project.ts`) — used in 21 places — absPath, relPath, zone
- **GuardInfo** (interface, `src/model/types.ts`) — used in 18 places — name, how, provider, path, line, confidence
- **BoundaryDetector** (interface, `src/analyze/boundaries/types.ts`) — used in 17 places — id
- **LevelNode** (interface, `web/src/types.ts`) — used in 14 places — childCount, drillable, outsideIn, outsideOut, preview
- **EnrichRequest** (interface, `src/enrich/types.ts`) — used in 13 places — system, user, maxOutputTokens
- **LabelItem** (interface, `src/enrich/prompts.ts`) — used in 12 places — key, path, zone, contains, responsibilities
- **StoreMeta** (interface, `src/model/types.ts`) — used in 12 places — storeKind, client, tables, reads, writes, sites
- **Tour** (interface, `web/src/types.ts`) — used in 12 places — id, title, subtitle, kind, steps
- **FieldInfo** (interface, `src/model/types.ts`) — used in 11 places — name, type, optional, isId?, isUnique?
- **ServiceMeta** (interface, `src/model/types.ts`) — used in 11 places — category, packages, hosts, sites, external
- **BoundaryFinding** (type-alias, `src/analyze/boundaries/types.ts`) — used in 14 places — | EndpointFinding | ServiceFinding | StoreFinding | EnvFinding | GuardFinding | WebhookFinding
- **BoundaryView** (interface, `web/src/types.ts`) — used in 10 places — appName, inputs, zones, outputs, flows, summary
- **InsightsView** (interface, `web/src/types.ts`) — used in 10 places — auth, services, stores, env
- **OverviewView** (interface, `web/src/types.ts`) — used in 10 places — meta, rootId, app, topLevel, busiestFiles, zoneCounts
- …and 171 more types.

## Environment variables

`ANTHROPIC_API_KEY`*, `ANTHROPIC_MODEL`*, `APP_ATLAS_DEBUG`*, `OPENAI_API_KEY`*, `OPENAI_BASE_URL`*, `OPENAI_MODEL`*, `RESEND_API_KEY`*, `STRIPE_SECRET_KEY`*, `STRIPE_WEBHOOK_SECRET`*

\* not in .env.example — 9 of 9 are read by the code but written down nowhere.

## Where to look first

The most connected files, which is usually where the app actually lives.

- `src/model/types.ts` (575 connections) — The Atlas data model.
- `web/src/types.ts` (339 connections) — Wire types.
- `src/analyze/ts/index.ts` (229 connections) — The TypeScript/JavaScript analyzer — the facts layer.
- `src/analyze/boundaries/build.ts` (209 connections) — Findings in, atlas nodes and edges out.
- `src/model/graph.ts` (205 connections) — In-memory query layer over an atlas.
- `src/enrich/index.ts` (192 connections) — The words layer.
- `src/analyze/boundaries/types.ts` (183 connections) — What a boundary detector produces, and what it is given to work with.
- `src/model/tours.ts` (168 connections) — Guided tours, derived rather than written (SPEC.md 6.4).

---

This file is generated. The full atlas — every file, function, type and edge — is in `.app-atlas/atlas.json`, and `app-atlas serve` opens the same information as a map you can click through.
