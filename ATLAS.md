# @app-atlas/cli — map

> Generated by App Atlas v0.25.0 on 2026-08-12. Facts are derived by each language's own parser and type checker; sentences marked `(ai)` were generated and may be wrong. Re-run `app-atlas export` after the code changes.

## What changed since the last run

Not recorded here. This file is committed, and what changed since the last run is a comparison against an atlas kept in `.app-atlas/`, which is not — so the answer depends on who ran it and when, and would be wrong for everyone else reading this. Run `app-atlas analyze` to see it for yourself.

## By the numbers

- 177 files · 54,104 lines · 20 folders · 1,285 functions · 382 types
- 4 ways in · 0 outside services · 4 data stores · 9 env vars
- Built with Actix Web, Cloudflare Pages, Cloudflare Workers, Diesel, React, Vite
- 99% of files carry a docstring App Atlas reads verbatim

## Ways in

Every one of the 1 routes is checked, or open on purpose.

| Kind | Route | Auth | Writes | Where |
|---|---|---|---|---|
| ANY | /* | checked | yes | test/fixtures/edge/wrangler.jsonc |

## Also runs on its own

- **cli** node csprobe.mjs
- **cli** python src/analyze/py/extract.py

## Where data goes

- **Files on disk** (Node fs and Python) — 38 reads, 17 writes
- **quiz-db** (Cloudflare D1) — 0 reads, 0 writes
- **OG_CACHE** (Cloudflare KV) — 0 reads, 0 writes
- **quiz-uploads** (Cloudflare R2) — 0 reads, 0 writes

## The parts

- `scripts` (3 files, logic)
- `src` (96 files, logic)
- `test` (58 files, test)
- `web/src` (18 files, ui)

## Files nothing else imports

Every one of the 101 source files here is imported by something else in the app, declared as a way in, or run by a framework convention.

## Key types

- **AtlasNode** (interface, `src/model/types.ts`) — used in 252 places — id, kind, name, label, parentId, language, path, startLine, endLine, zone, summary, summarySource
- **AtlasEdge** (interface, `src/model/types.ts`) — used in 90 places — id, kind, fromId, toId, weight, confidence, provenance, meta
- **BoundaryFinding** (type-alias, `src/analyze/boundaries/types.ts`) — used in 87 places — | EndpointFinding | ServiceFinding | StoreFinding | EnvFinding | GuardFinding | SignInCallFinding | WebhookFinding | ClientExportFinding | WrapperCallFinding | UrlSinkFinding | Ur…
- **DetectorContext** (interface, `src/analyze/boundaries/types.ts`) — used in 82 places — ref, sf, fileId, project, signals, imports, packages, locals
- **CodeSite** (interface, `src/model/types.ts`) — used in 75 places — path, line, nodeId, snippet?
- **AtlasGraph** (class, `src/model/graph.ts`) — used in 59 places — meta, rootId, nodes, children, relations, incoming, outgoing, pathCache
- **EndpointMeta** (interface, `src/model/types.ts`) — used in 53 places — endpointKind, method, route, framework, guards, writes, sites, open?, signInCall?, verified?, generatedEntry?, handlerUnlinked?
- **GuardInfo** (interface, `src/model/types.ts`) — used in 53 places — name, how, provider, path, line, confidence
- **Zone** (type-alias, `src/model/types.ts`) — used in 51 places — 'ui' | 'api' | 'logic' | 'data' | 'config' | 'test' | 'unknown'
- **GenericFile** (interface, `src/analyze/generic/ir.ts`) — used in 45 places — path, language, ok, error, doc, loc, namespace, imports, defs, calls, bindings, constants
- **AtlasNode** (interface, `web/src/types.ts`) — used in 44 places — id, kind, name, label, parentId, language, path, startLine, endLine, zone, summary, summarySource
- **GCall** (interface, `src/analyze/generic/ir.ts`) — used in 41 places — callee, method, receiver, args, line, scope, startIndex, endIndex
- **SourceFileRef** (interface, `src/analyze/project.ts`) — used in 41 places — absPath, relPath, zone
- **Atlas** (interface, `src/model/types.ts`) — used in 38 places — meta, nodes, edges
- **BoundaryDetector** (interface, `src/analyze/boundaries/types.ts`) — used in 26 places — id
- **EnrichBackend** (interface, `src/enrich/types.ts`) — used in 24 places — id, label, billing, pricing?, model?, concurrency
- **GDef** (interface, `src/analyze/generic/ir.ts`) — used in 24 places — kind, name, owner, line, endLine, startIndex, endIndex, doc, params, returns, exported, decorators
- **BoundaryInput** (interface, `src/analyze/generic/languages.ts`) — used in 23 places — file, fileId, signals
- **Dialect** (interface, `src/analyze/generic/dialect.ts`) — used in 23 places — id, displayName, extensions, skip?, strings, numbers, names, identifiers, qualified, functions, calls, comment
- **AtlasApp** (interface, `src/mcp/atlas.ts`) — used in 22 places — id, name, dir
- **Zone** (type-alias, `web/src/types.ts`) — used in 24 places — 'ui' | 'api' | 'logic' | 'data' | 'config' | 'test' | 'unknown'
- **Scope** (interface, `src/analyze/workspace.ts`) — used in 19 places — id, name, dir, kind
- **FieldInfo** (interface, `src/model/types.ts`) — used in 18 places — name, type, optional, isId?, isUnique?
- **LevelNode** (interface, `web/src/types.ts`) — used in 17 places — childCount, drillable, outsideIn, outsideOut, preview
- …and 358 more types.

## Environment variables

`ANTHROPIC_API_KEY`*, `ANTHROPIC_MODEL`*, `APP_ATLAS_DEBUG`*, `APP_ATLAS_PYTHON`*, `APP_ATLAS_PYTHON_TIMEOUT`*, `OPENAI_API_KEY`*, `OPENAI_BASE_URL`*, `OPENAI_MODEL`*, `PATH`†

\* not in .env.example — 8 of 9 are read by the code but written down nowhere.

† set by the hosting platform, not by you.

## Where to look first

The files that pull the most of this app together — entry points, and the modules that wire them up. Ranked by what they depend on, not by how many things touch them.

- `src/analyze/index.ts` (imports 23 files) — Analysis orchestrator.
- `src/main.ts` (imports 22 files) — The `app-atlas` command line.
- `src/analyze/generic/languages.ts` (imports 10 files) — Every language the generic tier can read, and what finds its boundaries.
- `src/mcp/index.ts` (imports 4 files) — `app-atlas mcp` — the atlas, answered over the Model Context Protocol.
- `web/src/App.tsx` (imports 14 files) — The app shell and its five lenses.
- `src/analyze/ts/index.ts` (imports 8 files) — The TypeScript/JavaScript analyzer — the facts layer.
- `src/analyze/boundaries/index.ts` (imports 11 files) — The boundary pass — one walk of a file, every detector watching.
- `src/analyze/generic/index.ts` (imports 12 files) — The generic tier — one plugin per language, all of them the same code.

---

This file is generated. The full atlas — every file, function, type and edge — is in `.app-atlas/atlas.json`, and `app-atlas serve` opens the same information as a map you can click through.
