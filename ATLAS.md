# app-atlas — map

> Generated by App Atlas v0.15.0 on 2026-08-02. Facts are derived by each language's own parser; sentences marked `(ai)` were generated and may be wrong. Re-run `app-atlas export` after the code changes.
>
> C# and Go were read with a tree-sitter grammar rather than a compiler. Structure, doors and checks come from the source as written; which declaration a name refers to is matched by name rather than resolved, so links between files are marked likely.

## What changed since the last run

Not recorded here. This file is committed, and what changed since the last run is a comparison against an atlas kept in `.app-atlas/`, which is not — so the answer depends on who ran it and when, and would be wrong for everyone else reading this. Run `app-atlas analyze` to see it for yourself.

## By the numbers

- 349 files · 49,281 lines · 161 folders · 1,471 functions · 425 types
- 66 ways in · 1 outside service · 5 data stores · 27 env vars
- Built with Cloudflare Pages, Cloudflare Workers, React, Vite
- 74% of files carry a docstring App Atlas reads verbatim · 21 describe code that has since changed
- **App Atlas could not read 1 file**, so whatever they declare — a route, a check, a table — is missing from everything below

## Ways in

Every one of the 47 routes has an auth check.
- App Atlas could not read 1 file; whatever they declare is missing from every number here.

| Kind | Route | Auth | Writes | Where |
|---|---|---|---|---|
| GET | / | custom? | — | test/fixtures/mounts/api/items.py |
| POST | / | custom? | yes | test/fixtures/gohttp/internal/api/router.go |
| PUT | /{artifact_hash}/upload | custom? | yes | test/fixtures/gochain/internal/pipeline/routes.go |
| GET | /{artifact_id}/download | custom? | — | test/fixtures/gochain/internal/pipeline/routes.go |
| GET | /{item_id} | custom? | — | test/fixtures/mounts/api/items.py |
| GET | /{name}/files | custom? | — | test/fixtures/gowrap/internal/packages/routes.go |
| POST | /{name}/files | custom? | yes | test/fixtures/gowrap/internal/packages/routes.go |
| GET | /{user_id} | custom? | — | test/fixtures/mounts/api/users.py |
| ANY | /* | custom? | yes | test/fixtures/edge/wrangler.jsonc |
| GET | /<order_id> | custom? | — | test/fixtures/flaskmounts/shop/orders.py |
| DELETE | /admin/orders/{id} | custom? | yes | test/fixtures/gohttp/internal/api/router.go |
| GET | /admin/status | custom? | — | test/fixtures/gomount/internal/admin/routes.go |
| GET | /anyone | custom? | — | test/fixtures/mounts/main.py |
| GET | /api/v1/orders/public-status | custom? | — | test/fixtures/csharpapi/src/Shop.Api/Controllers/OrdersController.cs |
| GET | /daily | custom? | — | test/fixtures/gochain/internal/reports/routes.go |
| ANY | /debug/vars | custom? | — | test/fixtures/gostd/main.go |
| GET | /health | custom? | — | test/fixtures/pyapp/app/main.py |
| GET | /healthz | custom? | — | test/fixtures/flaskmounts/app.py |
| GET | /items | custom? | — | test/fixtures/pyasgi/private_views.py |
| POST | /items | custom? | yes | test/fixtures/pyblind/app/routes.py |
| DELETE | /items/{item_id} | custom? | yes | test/fixtures/pyasgi/private_views.py |
| GET | /items/{item_id} | custom? | — | test/fixtures/pyorm/repo.py |
| PUT | /items/{item_id} | custom? | yes | test/fixtures/pyorm/repo.py |
| GET | /live | custom? | — | test/fixtures/mounts/main.py |
| POST | /login | custom? | yes | test/fixtures/pyasgi/public_views.py |
| GET | /monthly | custom? | — | test/fixtures/mounts/api/reports.py |
| GET | /orders | custom? | — | test/fixtures/gomount/internal/api/routes.go |
| POST | /orders | custom? | yes | test/fixtures/pyapp/app/main.py |
| GET | /profile | custom? | — | test/fixtures/pyauth/api/routes.py |
| POST | /purge | custom? | yes | test/fixtures/pyauth/api/routes.py |
| GET | /register | custom? | — | test/fixtures/gochain/internal/runner/routes.go |
| GET | /reports | custom? | — | test/fixtures/pyauth/api/controllers.py |
| POST | /reports | custom? | yes | test/fixtures/goecho/server.go |
| GET | /reports/:id | custom? | — | test/fixtures/goecho/server.go |
| DELETE | /reports/{report_id} | custom? | yes | test/fixtures/pyauth/api/controllers.py |
| GET | /settings | custom? | — | test/fixtures/pyasgi/admin_views.py |
| GET | /status | custom? | — | test/fixtures/pyauth/api/routes.py |
| GET | /tenant | custom? | — | test/fixtures/pyauth/api/routes.py |
| GET | /token | custom? | — | test/fixtures/gowrap/internal/packages/routes.go |
| GET | /users | custom? | — | test/fixtures/pyapp/app/main.py |
| … | 7 more | | | |

## Also runs on its own

- **webhook** /webhooks/stripe — `test/fixtures/pyapp/app/main.py`
- **queue** email_digest — `test/fixtures/pyapp/app/tasks.py`
- **cli** node scripts/fetch-grammars.mjs
- **cli** python src/analyze/py/extract.py
- **cli** python test/fixtures/pycli/scripts/release/bump_version.py
- **cli** python test/fixtures/pycli/scripts/release/tag_release.py
- **cli** 9 scripts under `test/fixtures/pycli/scripts/verify`, run one at a time — `census_probe.py`, `columns_probe.py`, `crews_probe.py`, and 6 more
- **cli** python test/fixtures/pycli/toolkit/cli.py
- **cli** python test/fixtures/pypackage/toolkit/format.py
- **cli** python test/fixtures/pyscripts/clean.py
- **cli** python test/fixtures/pyscripts/report.py

## Where data goes

- **Files on disk** (Node fs and Python) — 31 reads, 16 writes
- **CSV files** (pandas) — 1 reads, 0 writes
- **quiz-db** (Cloudflare D1) — 0 reads, 0 writes
- **OG_CACHE** (Cloudflare KV) — 0 reads, 0 writes
- **quiz-uploads** (Cloudflare R2) — 0 reads, 0 writes
- **api.weather.gov** (other) — 1 call site via requests, api.weather.gov

## The parts

- `scripts` (3 files, logic)
- `src` (88 files, logic)
- `test` (244 files, logic)
- `web/src` (14 files, ui)

## Files nothing else imports

Every one of the 91 source files here is imported by something else in the app, declared as a way in, or run by a framework convention.

## Key types

- **AtlasNode** (interface, `src/model/types.ts`) — used in 238 places — id, kind, name, label, parentId, language, path, startLine, endLine, zone, summary, summarySource
- **AtlasEdge** (interface, `src/model/types.ts`) — used in 86 places — id, kind, fromId, toId, weight, confidence, provenance, meta
- **DetectorContext** (interface, `src/analyze/boundaries/types.ts`) — used in 79 places — ref, sf, fileId, project, signals, imports, packages, locals
- **BoundaryFinding** (type-alias, `src/analyze/boundaries/types.ts`) — used in 71 places — | EndpointFinding | ServiceFinding | StoreFinding | EnvFinding | GuardFinding | SignInCallFinding | WebhookFinding | ClientExportFinding | WrapperCallFinding | UrlSinkFinding | Ur…
- **CodeSite** (interface, `src/model/types.ts`) — used in 64 places — path, line, nodeId, snippet?
- **AtlasGraph** (class, `src/model/graph.ts`) — used in 59 places — meta, rootId, nodes, children, relations, incoming, outgoing, pathCache
- **EndpointMeta** (interface, `src/model/types.ts`) — used in 53 places — endpointKind, method, route, framework, guards, writes, sites, open?, signInCall?, verified?, schedule?, vars?
- **GuardInfo** (interface, `src/model/types.ts`) — used in 49 places — name, how, provider, path, line, confidence
- **Zone** (type-alias, `src/model/types.ts`) — used in 50 places — 'ui' | 'api' | 'logic' | 'data' | 'config' | 'test' | 'unknown'
- **SourceFileRef** (interface, `src/analyze/project.ts`) — used in 40 places — absPath, relPath, zone
- **Atlas** (interface, `src/model/types.ts`) — used in 38 places — meta, nodes, edges
- **AtlasNode** (interface, `web/src/types.ts`) — used in 37 places — id, kind, name, label, parentId, language, path, startLine, endLine, zone, summary, summarySource
- **GenericFile** (interface, `src/analyze/generic/ir.ts`) — used in 35 places — path, language, ok, error, doc, loc, namespace, imports, defs, calls, bindings, constants
- **GCall** (interface, `src/analyze/generic/ir.ts`) — used in 31 places — callee, method, receiver, args, line, scope, startIndex, endIndex
- **BoundaryDetector** (interface, `src/analyze/boundaries/types.ts`) — used in 25 places — id
- **AtlasApp** (interface, `src/mcp/atlas.ts`) — used in 22 places — id, name, dir
- **GDef** (interface, `src/analyze/generic/ir.ts`) — used in 22 places — kind, name, owner, line, endLine, startIndex, endIndex, doc, params, returns, exported, fields
- **EnrichBackend** (interface, `src/enrich/types.ts`) — used in 21 places — id, label, billing, pricing?, model?, concurrency
- **Dialect** (interface, `src/analyze/generic/dialect.ts`) — used in 18 places — id, displayName, extensions, skip?, strings, numbers, names, identifiers, qualified, functions, calls, comment
- **StoreMeta** (interface, `src/model/types.ts`) — used in 17 places — storeKind, client, tables, catalogTables, reads, writes, sites
- **FieldInfo** (interface, `src/model/types.ts`) — used in 16 places — name, type, optional, isId?, isUnique?
- **ProjectSignals** (interface, `src/analyze/signals.ts`) — used in 16 places — packages, pythonPackages, pythonManifest, goModules, goModule, dotnetPackages, dotnetSdks, declaresAPackage, entryPoints, nextAppDir, nextPagesDir, expoRouterDir
- **PyFile** (interface, `src/analyze/py/types.ts`) — used in 16 places — path, ok, error, doc?, loc?, imports?, defs?, calls?, subscripts?, aliases?, bindings?, routers?
- **BoundaryInput** (interface, `src/analyze/generic/languages.ts`) — used in 15 places — file, fileId, signals
- …and 401 more types.

## Environment variables

`ANTHROPIC_API_KEY`*, `ANTHROPIC_MODEL`*, `API_TOKEN`*, `APP_ATLAS_DEBUG`*, `APP_ATLAS_PYTHON`*, `APP_ATLAS_PYTHON_TIMEOUT`*, `DATABASE_URL`*, `GITHUB_CLIENT_SECRET`*, `MOUNT_AT`*, `MYSQL_HOST`*, `MYSQL_PASSWORD`*, `MYSQL_USER`*, `NEXT_PUBLIC_APP_URL`*, `NEXT_PUBLIC_SUPABASE_ANON_KEY`*, `NODE_ENV`†, `OPENAI_API_KEY`*, `OPENAI_BASE_URL`*, `OPENAI_MODEL`*, `PATH`†, `PORT`†, `PRICING_API_KEY`*, `RESEND_API_KEY`*, `STRIPE_SECRET_KEY`*, `STRIPE_WEBHOOK_SECRET`*, `SUPABASE_ANON_KEY`*, `SUPABASE_URL`*, `VERCEL_URL`†

\* not in .env.example — 23 of 27 are read by the code but written down nowhere.

† set by the hosting platform, not by you.

## Where to look first

The files that pull the most of this app together — entry points, and the modules that wire them up. Ranked by what they depend on, not by how many things touch them.

- `src/analyze/index.ts` (imports 21 files) — Analysis orchestrator.
- `src/cli.ts` (imports 18 files) — The `app-atlas` command line.
- `src/mcp/index.ts` (imports 4 files) — `app-atlas mcp` — the atlas, answered over the Model Context Protocol.
- `src/analyze/generic/languages.ts` (imports 8 files) — Every language the generic tier can read, and what finds its boundaries.
- `web/src/App.tsx` (imports 11 files) — The app shell and its five lenses.
- `src/analyze/ts/index.ts` (imports 8 files) — The TypeScript/JavaScript analyzer — the facts layer.
- `src/analyze/boundaries/index.ts` (imports 11 files) — The boundary pass — one walk of a file, every detector watching.
- `src/analyze/generic/index.ts` (imports 11 files) — The generic tier — one plugin per language, all of them the same code.

---

This file is generated. The full atlas — every file, function, type and edge — is in `.app-atlas/atlas.json`, and `app-atlas serve` opens the same information as a map you can click through.
