# app-atlas — map

> Generated by App Atlas v0.6.0 on 2026-07-31. Facts are derived by each language's own parser; sentences marked `(ai)` were generated and may be wrong. Re-run `app-atlas export` after the code changes.
>
> Go was read with a tree-sitter grammar rather than a compiler. Structure, doors and checks come from the source as written; which declaration a name refers to is matched by name rather than resolved, so links between files are marked likely.

## What changed since the last run

**8 new doors since the last run — 6 of them with no auth check App Atlas can see.**

- `POST /{name}/files` — writes data — `test/fixtures/gowrap/internal/packages/routes.go:14`
- `GET /{name}/files` — `test/fixtures/gowrap/internal/packages/routes.go:13`
- `GET /admin/status` — `test/fixtures/gomount/internal/admin/routes.go:12`
- `GET /orders` — `test/fixtures/gomount/internal/api/routes.go:9`
- `GET /token` — `test/fixtures/gowrap/internal/packages/routes.go:22`
- `GET /version` — `test/fixtures/gowrap/internal/api/routes.go:25`

5 doors changed, 36 new files, 16 files changed, 125 new functions, 13 functions changed, and 3 more kinds of change.

## By the numbers

- 258 files · 36,380 lines · 118 folders · 1,129 functions · 351 types
- 48 ways in · 3 outside services · 6 data stores · 24 env vars
- Built with Cloudflare Pages, Cloudflare Workers, React, Vite
- 70% of files carry a docstring App Atlas reads verbatim · 109 describe code that has since changed

## Ways in

**28 of 41 routes have no auth check App Atlas can see.** They are listed first.
- 2 more lean on a file App Atlas could not read — the check may well be in there.
- App Atlas could not read 1 file; whatever they declare is missing from every number here.

| Kind | Route | Auth | Writes | Where |
|---|---|---|---|---|
| POST | /{name}/files | **none found** | yes | test/fixtures/gowrap/internal/packages/routes.go |
| ANY | /* | **none found** | yes | test/fixtures/edge/wrangler.jsonc |
| DELETE | /items/{item_id} | **none found** | yes | test/fixtures/pyasgi/private_views.py |
| PUT | /items/{item_id} | **none found** | yes | test/fixtures/pyorm/repo.py |
| POST | /login | **none found** | yes | test/fixtures/pyasgi/public_views.py |
| POST | /purge | **none found** | yes | test/fixtures/pyauth/api/routes.py |
| POST | /webhooks/stripe | **none found** | yes | test/fixtures/pyapp/app/main.py |
| GET | / | **none found** | — | test/fixtures/mounts/api/items.py |
| GET | /{item_id} | **none found** | — | test/fixtures/mounts/api/items.py |
| GET | /{name}/files | **none found** | — | test/fixtures/gowrap/internal/packages/routes.go |
| GET | /{user_id} | **none found** | — | test/fixtures/mounts/api/users.py |
| GET | /<order_id> | **none found** | — | test/fixtures/flaskmounts/shop/orders.py |
| GET | /admin/status | **none found** | — | test/fixtures/gomount/internal/admin/routes.go |
| GET | /anyone | **none found** | — | test/fixtures/mounts/main.py |
| ANY | /debug/vars | **none found** | — | test/fixtures/gostd/main.go |
| GET | /health | **none found** | — | test/fixtures/pyapp/app/main.py |
| GET | /healthz | **none found** | — | test/fixtures/flaskmounts/app.py |
| GET | /items/{item_id} | **none found** | — | test/fixtures/pyorm/repo.py |
| GET | /live | **none found** | — | test/fixtures/mounts/main.py |
| GET | /monthly | **none found** | — | test/fixtures/mounts/api/reports.py |
| GET | /orders | **none found** | — | test/fixtures/gomount/internal/api/routes.go |
| GET | /orders/ | **none found** | — | test/fixtures/gohttp/internal/api/router.go |
| GET | /settings | **none found** | — | test/fixtures/pyasgi/admin_views.py |
| GET | /status | **none found** | — | test/fixtures/pyauth/api/routes.py |
| GET | /tenant | **none found** | — | test/fixtures/pyauth/api/routes.py |
| GET | /token | **none found** | — | test/fixtures/gowrap/internal/packages/routes.go |
| GET | /users | **none found** | — | test/fixtures/pyapp/app/main.py |
| GET | /version | **none found** | — | test/fixtures/gowrap/internal/api/routes.go |
| GET | /items | not examined | — | test/fixtures/pyasgi/private_views.py |
| POST | /items | not examined | yes | test/fixtures/pyblind/app/routes.py |
| DELETE | /admin/orders/{id} | custom? | yes | test/fixtures/gohttp/internal/api/router.go |
| POST | /orders | custom? | yes | test/fixtures/pyapp/app/main.py |
| POST | /orders/ | custom? | yes | test/fixtures/gohttp/internal/api/router.go |
| GET | /profile | custom? | — | test/fixtures/pyauth/api/routes.py |
| GET | /reports | custom? | — | test/fixtures/pyauth/api/controllers.py |
| POST | /reports | custom? | yes | test/fixtures/goecho/server.go |
| GET | /reports/:id | custom? | — | test/fixtures/goecho/server.go |
| DELETE | /reports/{report_id} | custom? | yes | test/fixtures/pyauth/api/controllers.py |
| ANY | /functions/v1/greet | Supabase | — | test/fixtures/boundary/supabase/functions/greet/index.ts |
| ANY | /functions/v1/sync | Supabase | — | test/fixtures/expo/supabase/functions/sync/index.ts |
| … | 1 more | | | |

## Also runs on its own

- **cli** Command line — `scripts/fetch-grammars.mjs`
- **cli** src/analyze/py/extract.py — `src/analyze/py/extract.py`
- **cli** test/fixtures/pypackage/toolkit/format.py — `test/fixtures/pypackage/toolkit/format.py`
- **cli** test/fixtures/pyscripts/clean.py — `test/fixtures/pyscripts/clean.py`
- **cli** test/fixtures/pyscripts/report.py — `test/fixtures/pyscripts/report.py`
- **webhook** /webhooks/stripe — `test/fixtures/pyapp/app/main.py`
- **queue** email_digest — `test/fixtures/pyapp/app/tasks.py`

## Where data goes

- **Files on disk** (Node fs and Python) — 28 reads, 16 writes
- **Database** (database/sql) — 1 reads, 2 writes — tables: orders
- **CSV files** (pandas) — 1 reads, 0 writes
- **quiz-db** (Cloudflare D1) — 0 reads, 0 writes
- **OG_CACHE** (Cloudflare KV) — 0 reads, 0 writes
- **quiz-uploads** (Cloudflare R2) — 0 reads, 0 writes
- **api.weather.gov** (other) — 1 call site via requests, api.weather.gov
- **Slack** (other) — 1 call site via net/http, hooks.slack.com
- **Stripe** (payments) — 1 call site via stripe

## The parts

- `scripts` (3 files, logic)
- `src` (73 files, logic)
- `test` (168 files, api)
- `web/src` (14 files, ui)

## Database tables

- **orders** — columns unknown

## Key types

- **AtlasNode** (interface, `src/model/types.ts`) — used in 183 places — id, kind, name, label, parentId, language, path, startLine, endLine, zone, summary, summarySource
- **DetectorContext** (interface, `src/analyze/boundaries/types.ts`) — used in 75 places — ref, sf, fileId, project, signals, imports, packages, locals
- **AtlasEdge** (interface, `src/model/types.ts`) — used in 70 places — id, kind, fromId, toId, weight, confidence, provenance, meta
- **BoundaryFinding** (type-alias, `src/analyze/boundaries/types.ts`) — used in 59 places — | EndpointFinding | ServiceFinding | StoreFinding | EnvFinding | GuardFinding | WebhookFinding | ClientExportFinding | WrapperCallFinding | AuthCheckerFinding | AuthAliasFinding |…
- **CodeSite** (interface, `src/model/types.ts`) — used in 49 places — path, line, nodeId, snippet?
- **GuardInfo** (interface, `src/model/types.ts`) — used in 44 places — name, how, provider, path, line, confidence
- **AtlasGraph** (class, `src/model/graph.ts`) — used in 40 places — meta, rootId, nodes, children, relations, incoming, outgoing, pathCache
- **EndpointMeta** (interface, `src/model/types.ts`) — used in 40 places — endpointKind, method, route, framework, guards, writes, sites, open?, verified?, schedule?, vars?, envExample?
- **SourceFileRef** (interface, `src/analyze/project.ts`) — used in 40 places — absPath, relPath, zone
- **Zone** (type-alias, `src/model/types.ts`) — used in 44 places — 'ui' | 'api' | 'logic' | 'data' | 'config' | 'test' | 'unknown'
- **Atlas** (interface, `src/model/types.ts`) — used in 38 places — meta, nodes, edges
- **AtlasNode** (interface, `web/src/types.ts`) — used in 37 places — id, kind, name, label, parentId, language, path, startLine, endLine, zone, summary, summarySource
- **GenericFile** (interface, `src/analyze/generic/ir.ts`) — used in 26 places — path, language, ok, error, doc, loc, namespace, imports, defs, calls, bindings, constants
- **BoundaryDetector** (interface, `src/analyze/boundaries/types.ts`) — used in 25 places — id
- **EnrichBackend** (interface, `src/enrich/types.ts`) — used in 21 places — id, label, billing, pricing?, model?, concurrency
- **GDef** (interface, `src/analyze/generic/ir.ts`) — used in 20 places — kind, name, owner, line, endLine, startIndex, endIndex, doc, params, returns, exported, fields
- **FieldInfo** (interface, `src/model/types.ts`) — used in 16 places — name, type, optional, isId?, isUnique?
- **FileSlice** (interface, `src/analyze/plugin.ts`) — used in 15 places — relPath, hash, nodes, edges, boundaries, positions, imports
- **GCall** (interface, `src/analyze/generic/ir.ts`) — used in 15 places — callee, method, receiver, args, line, scope, startIndex, endIndex
- **PyFile** (interface, `src/analyze/py/types.ts`) — used in 15 places — path, ok, error, doc?, loc?, imports?, defs?, calls?, subscripts?, aliases?, bindings?, routers?
- **PythonBoundaryInput** (interface, `src/analyze/py/boundaries.ts`) — used in 15 places — file, fileId, nodeIdForScope, packages
- **StoreMeta** (interface, `src/model/types.ts`) — used in 15 places — storeKind, client, tables, reads, writes, sites
- **Tour** (interface, `web/src/types.ts`) — used in 15 places — id, title, subtitle, kind, steps
- …and 327 more types.

## Environment variables

`ANTHROPIC_API_KEY`*, `ANTHROPIC_MODEL`*, `API_TOKEN`*, `APP_ATLAS_DEBUG`*, `APP_ATLAS_PYTHON`*, `DATABASE_URL`*, `GITHUB_CLIENT_SECRET`*, `MOUNT_AT`*, `MYSQL_HOST`*, `MYSQL_PASSWORD`*, `MYSQL_USER`*, `NEXT_PUBLIC_APP_URL`*, `NEXT_PUBLIC_SUPABASE_ANON_KEY`*, `NODE_ENV`†, `OPENAI_API_KEY`*, `OPENAI_BASE_URL`*, `OPENAI_MODEL`*, `PORT`†, `RESEND_API_KEY`*, `STRIPE_SECRET_KEY`*, `STRIPE_WEBHOOK_SECRET`*, `SUPABASE_ANON_KEY`*, `SUPABASE_URL`*, `VERCEL_URL`†

\* not in .env.example — 21 of 24 are read by the code but written down nowhere.

† set by the hosting platform, not by you.

## Where to look first

The most connected files, which is usually where the app actually lives.

- `src/model/types.ts` (973 connections) — The Atlas data model.
- `web/src/types.ts` (435 connections) — Wire types.
- `src/analyze/boundaries/build.ts` (416 connections) — Findings in, atlas nodes and edges out.
- `src/analyze/boundaries/types.ts` (374 connections) — What a boundary detector produces, and what it is given to work with.
- `src/analyze/py/boundaries.ts` (304 connections) — Where data enters and leaves a Python app.
- `src/analyze/ts/index.ts` (286 connections) — The TypeScript/JavaScript analyzer — the facts layer.
- `src/model/graph.ts` (249 connections) — In-memory query layer over an atlas.
- `src/cli.ts` (209 connections) — The `app-atlas` command line.

---

This file is generated. The full atlas — every file, function, type and edge — is in `.app-atlas/atlas.json`, and `app-atlas serve` opens the same information as a map you can click through.
